import React, { Fragment } from 'react';
import { GlobalStyle } from 'theme';

export const App: React.FC = () => {
  return (
    <Fragment>
      <GlobalStyle />
      <div>App</div>
    </Fragment>
  );
};
